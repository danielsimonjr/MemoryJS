/**
 * Event Reification Module — Barrel Export (R1)
 *
 * @module agent/events
 */

export {
  EventManager,
  eventEntityName,
  EVENT_ENTITY_TYPE,
  EVENT_STUB_ENTITY_TYPE,
  EVENT_TAG,
  FLOW_TAG_PREFIX,
  ACTOR_OF_RELATION,
  TARGETED_RELATION,
  OCCURRED_IN_RELATION,
  PARTICIPANT_IN_RELATION,
  type EventManagerConfig,
} from './EventManager.js';
