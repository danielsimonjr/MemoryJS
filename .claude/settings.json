{
  "project": {
    "name": "memoryjs",
    "description": "TypeScript knowledge graph library for managing entities, relations, and observations with advanced search capabilities",
    "type": "typescript-library"
  },
  "enabledServers": [
    "deepthinking-mcp",
    "everything-mcp",
    "fzf-mcp",
    "memory-mcp"
  ],
  "buildRequirements": {
    "typescript": {
      "buildCommand": "bun run build",
      "entryPoint": "dist/",
      "typecheckCommand": "bun run typecheck"
    }
  },
  "testing": {
    "framework": "vitest",
    "command": "bun run test",
    "coverageCommand": "bun run test:coverage"
  },
  "documentation": {
    "claudemd": "CLAUDE.md",
    "architecture": "docs/architecture/"
  }
}
